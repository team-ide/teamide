name: user/queryByAccount
args:
  - name: account
    type: user.account
steps:

  # 查询账号
  - db: selectOne
    table: TB_USER
    wheres:
      - name: account
        value: account
    setVar: user
    setVarType: user

return: user