package golang

import (
	"teamide/pkg/maker/coder"
	"teamide/pkg/maker/modelers"
)

type CodeBuilder struct {
	*coder.Builder
}

func (this_ *Generator) AppendFunc(funcName string, args []*modelers.ArgModel, code string) (err error) {

	return
}
