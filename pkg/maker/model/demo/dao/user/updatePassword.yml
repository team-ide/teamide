name: user/updatePassword
args:
  - name: userId
    type: user.userId
  - name: salt
    type: user.salt
  - name: password
    type: user.password
steps:

  # 更新
  - db: update
    table: TB_USER
    columns:
      - name: salt
        value: salt
      - name: password
        value: password
    wheres:
      - name: userId
        value: userId
    setVar: updateCount

return: updateCount